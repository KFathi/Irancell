package com.hoor.lostfinder;

import android.app.AlertDialog;
import android.content.Context;
import android.graphics.Color;
import android.preference.DialogPreference;
import android.util.AttributeSet;
import android.widget.Button;

public class LostDialgPreference extends DialogPreference {

public LostDialgPreference(Context context, AttributeSet attrs) {
    super(context, attrs);

    //setDialogLayoutResource(R.layout.numberpicker_dialog);
    //setPositiveButtonText(android.R.string.paste);
    //setNegativeButtonText(android.R.string.cancel);
    //Button button = ((AlertDialog) getDialog()).getButton(AlertDialog.BUTTON_POSITIVE);
    //button.setBackgroundColor(Color.GREEN);
     
}
}
